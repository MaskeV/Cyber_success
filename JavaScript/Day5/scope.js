

let num=10;


function  doSomething(){
    let num =20;   //shadowding   In JavaScript, variable shadowing occurs when a variable declared within a specific inner scope (like a function or a block) has the same name as a variable in an outer scope (global or parent scope). The inner variable "shadows" the outer one, meaning that code within the inner scope will access the inner variable, while the outer variable remains unaffected and inaccessible within that inner scope
    console.log


}